---
description: 房间生命周期、断线重连、超时托管的既有行为，改之前先对照
globs: src/SkullKing.Application/Rooms/**
alwaysApply: false
---

# 房间生命周期与托管

改这块之前先读懂现状，很多分支是踩过坑才长这样的。

## 时间常量

| 常量 | 值 | 位置 |
| --- | --- | --- |
| 不限时房间的掉线托管宽限 | 2 分钟 | `RoomService.Game.cs` `DisconnectGrace` |
| 非对局房间清人 | 10 分钟 | `RoomService.Game.cs` `AbandonGrace` |
| 重启后不托管的窗口 | 90 秒 | `RoomService.Restore.cs` `ResumeGrace` |
| 判定掉线 | 2 分钟 | `Program.cs` 与 `client/src/api/hub.ts`，两端要一致 |

## 不变量

- **回收条件只有一个**：成员数归零。没有空闲超时，有人在房间就一直留着。
- **对局中不删参战玩家**，退出和掉线都只标记离线并保留座位。所以 `SweepAbandonedAsync` 只在非对局房间跑。
- **托管触发**：设了限时只看 `TurnDeadline` 到没到，掉线不加速；不限时才用 `DisconnectGrace` 兜底。
- **`TurnDeadline` 必须在每个改变点重算**：出牌后（`PublishAsync`）、改限时后（`UpdateSettingsAsync`）、重启恢复后（`Rebuild`）、掉线的人重连回来且牌局正等他时（`JoinAsync`）。漏掉就等于永久卡桌。
- **同一条连接换房要先摘旧房**。`_connections` 只保留最新映射，不摘的话旧房间会留下一个永远「在线」的成员，房间清不掉也回收不了。

## 恢复范围

`LoadResumableRoomsAsync` 捞的是 `Status != Finished`，等人中的房间也要恢复——只恢复对局中的话，每次发版所有刚建好的房间都会消失。恢复出来的成员一律是掉线状态，靠 10 分钟清人自愈。

## 口令

校验一律走 `PasswordHash`（加盐 SHA256，不可逆）。`RoomSettings.Password` 是明文，落库了，唯一用途是把口令拼进邀请链接，只发给已经在房里的人。

---
description: 骷髅王项目的通用约定：中文、注释风格、提交信息、伪装术语
alwaysApply: true
---

# 通用约定

- 回答一律用中文。
- 先读根目录的 `AGENTS.md`，那里有命令和五条铁律。

## 注释

只写「为什么」，不写「是什么」。值得留注释的是：踩过的坑、被否掉的方案、看起来多余但不能删的代码。

```csharp
// ❌ 把成员标记为掉线
member.DisconnectedAt = Now;

// ✅ 断线一律先留人。刷新页面、切后台被浏览器掐掉心跳、地铁里断一下网都会走到
// 这里，立刻清出去的话一个人的房间会当场被回收，人再回来只看到「房间不存在」。
member.DisconnectedAt = Now;
```

## 提交信息

一句中文陈述句，说清改了什么，不带前缀。参考现有历史：

```
修掉整页滚动条与成员列表错位，补一份新人指引
群设置每项加说明气泡，自动伪装改为默认关闭
邀请链接带上口令，修掉换群不退旧群留下在线幽灵
```

改完并通过验证后直接 `git commit` 再 `git push` 到当前分支，不必等人说「提交」。不要用 `--force`，不要改 git config。

## 伪装术语

面向用户的文案不许出现牌桌词汇。对照关系在 `client/src/skins/im/vocabulary.ts`：房间＝群聊，玩家＝群成员，出牌＝发消息，叫牌＝承接量／本周可承接，得分＝绩效，开局＝发起议程，轮次＝议程轮数。不要写「接龙」。

写新文案时先去词表里找现成说法，没有再造，造完补进词表。

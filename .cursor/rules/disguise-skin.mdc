---
description: 伪装皮肤的边界：术语、无干扰、可换皮
globs: client/src/skins/**
alwaysApply: false
---

# 伪装皮肤

这一层的存在意义是「让人看不出在玩游戏」。任何改动都要过这三条。

## 术语

面向用户的字一律用办公黑话，别写牌桌词汇。先查 `vocabulary.ts` 有没有现成说法，没有再造，造完补进去。牌面文案统一走 `describeCard`，不要在组件里硬编码牌名。

## 不引人注意

- 不弹浏览器原生对话框（`alert` / `confirm`）。要确认就用 `ModalShell`。
- 不发声音，不用刺眼的红黄大色块。轮到你行动只用一个很淡的提示。
- 未读数在伪装态下不许出现在 `document.title` 里，那是最容易被瞥见的地方。

## 可换皮

游戏逻辑不许写进皮肤组件。皮肤只实现 `SkinDefinition`（`skins/types.ts`）要求的那几个组件，通过 store 拿数据。判断「这段代码换一套皮肤还需不需要」——需要的话它就该待在 `stores/` 或 `api/`。

新增皮肤只要在 `skins/registry.ts` 里注册，不用动任何游戏代码。

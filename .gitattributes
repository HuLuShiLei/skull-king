# 让 git 自己判断哪些是文本文件，并在仓库里统一存成 LF。
* text=auto

# 这几类文件必须是 LF：容器里是 /bin/sh 在执行它们，
# 带 CR 会得到 "no such file or directory" 这种毫无线索的报错。
*.sh text eol=lf
Dockerfile* text eol=lf
*.conf text eol=lf
*.yml text eol=lf
*.yaml text eol=lf

# 二进制，别做任何转换
*.png binary
*.jpg binary
*.svg text
*.db binary
